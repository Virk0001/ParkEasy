<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>💳 Payment Gateway</title>
  <link rel="stylesheet" href="/main/style.css" />
</head>
<body class="bg-slate-900 text-gray-100 min-h-screen font-sans flex flex-col items-center justify-center">
  <header class="bg-slate-800 shadow-lg py-6 px-8 w-full max-w-md rounded-t-lg text-center">
    <h1 class="text-2xl font-semibold text-green-400">💳 Secure Payment Gateway</h1>
    <form action="/dashboard" method="get" class="mt-4">
      <button type="submit" id="backBtn" class="bg-slate-700 hover:bg-green-500 hover:text-white text-green-400 font-semibold text-base px-5 py-2 rounded-md transition-colors">⬅ Back to Dashboard</button>
    </form>
  </header>

  <main class="bg-slate-800 shadow-lg p-8 w-full max-w-md rounded-b-lg">
    <form id="paymentForm" class="space-y-6">
      <p class="text-lg"><strong>Amount to Pay:</strong> ₹<span id="amountDisplay" class="text-green-400">...</span></p>

      <div>
        <label for="method" class="block text-sm font-medium text-gray-300 mb-1">Select Payment Method:</label>
        <select id="method" required class="w-full px-3 py-2 bg-slate-700 border border-slate-600 rounded-md text-gray-100 focus:ring-green-500 focus:border-green-500 transition-colors">
          <option value="">--Choose--</option>
          <option value="card">💳 Card</option>
          <option value="upi">📱 UPI</option>
          <option value="wallet">👛 Wallet</option>
        </select>
      </div>

      <div id="detailsContainer" class="space-y-4">
        <!-- Payment method specific fields will be injected here by JavaScript -->
      </div>

      <button type="submit" class="w-full bg-green-500 hover:bg-green-600 text-white font-semibold py-3 px-5 rounded-md shadow-md hover:shadow-lg transition duration-150 ease-in-out">Pay Now</button>
    </form>

    <div id="confirmation" style="display:none;" class="mt-8 p-6 bg-green-600 text-white rounded-lg shadow-xl text-center">
      <p class="text-xl font-semibold">✅ Payment Successful!</p>
      <p>Amount: ₹<span id="confirmAmount"></span></p>
      <p class="mt-2 text-sm">Receipt will be sent to your registered email.</p>
    </div>
  </main>

  <script src="/js/payment_gateway.js" defer></script>
</body>
</html>
